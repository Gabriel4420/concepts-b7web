/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['avatars.githubusercontent.com', 'th.bing.com', 'images.unsplash.com', 'wallpapercave.com']
    }
};

export default nextConfig;
